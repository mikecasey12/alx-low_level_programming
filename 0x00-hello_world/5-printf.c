#include<stdio.h>

/**
 * main - Prints message using printf
 * Return: Returns the message
 */
int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
